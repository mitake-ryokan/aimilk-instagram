# サスケのボドゲ棚 — 運用メモ

毎週土曜20:00 JST に「サスケのボドゲ棚」を自動投稿する枠の運用知識。
コードやコミット履歴からは読み取れない「決めごと」だけをここに残す。
（出典: MITAKECATSセッションからの組み込み依頼 INTEGRATION.md を要約。素材一式は
配置済みのため元フォルダ sasuke_module は破棄した。）

## 仕組みの在りか

- 画像生成: `sasuke/make_card.py`（1080×1350 JPEG・自己完結）
- 写真: `sasuke/photos/vol01.jpg` のように置くと1枚目の写真として使われる。
  ゲーム名でのファイル名（`宝石の煌き.jpg`）でもよい。
  **無くてもよい**。無い週は深緑＋ロゴの代替パネルが自動で入る。
- 投稿本体: `sasuke/run_sasuke.py`（未投稿の最小volを取る→カード生成→
  [カード, 締めロゴ]の2枚カルーセルを投稿→state記録）
- 投稿キュー: `sasuke/queue.json`（vol / game / serif / caption）
- ゲーム一覧: `sasuke/games.csv`（ゲーム名 / プレイ人数 / プレイ時間 / 対象年齢 / 発売年）
- 素材: `sasuke/assets/`（sasuke_face.png / mitakecats_logo.png / closing.jpg）
- ワークフロー: `.github/workflows/sasuke-weekly.yml`（cron `0 11 * * 6`＝土20:00 JST）
- 投稿済み管理: `state/sasuke_posted.json`（自動生成・自動コミット）
- トークンは新設せず、みるくと同じ `IG_ACCESS_TOKEN` を自己延長して使い回す。

## vol の採番

- **vol.01 から始まる**（2026-09-04 変更）。
- もともと「vol.01〜04 は手動で予約投稿済み、キューは vol.05 から」という
  前提でキューを作っていたが、3号さんに確認したところ手動分は出していない。
  そこでキューを vol.05〜16 から **vol.01〜12** に振り直した。
- 既出として避けるゲーム: カタン・おばけキャッチ・ガイスター・ニムト
  （番号は付いていないが、別枠で扱った分。次の12週を選ぶときは重複させない）

## キャプションの扱い

- `queue.json` の `caption` は **無編集でそのまま投稿**する。サスケの口調ルールが
  焼き込んであるため、こちらで手を入れない。
- 将来こちら側でキャプションを書く場合の口調ルール:
  - 一人称は「僕」
  - 語尾に控えめな「にゃ」（文末中心。ナ行を稀に「にゃ」化する程度）
  - 冷静な解説役。誇張しない
  - 最後は必ず定型文（MITAKECATSの注記）とハッシュタグで締める
- ゲームのルール記述は12本とも裏取り済み。変更するとルール誤記のリスクがあるので触らない。
- 場所の呼称は「プレイルーム」。**「蔵」という場所はみたけに実在しない**ので使わない。

## キューが切れたら（12週後）

3号さんが MITAKECATSセッション（Cowork）に「サスケの続き」と言えば、
次の12週分の `queue.json` を同形式で生成して渡す。選定ルール:

- `boardgames.csv` にある物だけを選ぶ
- 既出 vol.01〜16（カタン・おばけキャッチ・ガイスター・ニムト＋本キュー12本）と重複させない
- 月単位で「定番・家族・2人用・大人数」のバランスを取る

## デザイン（2026-09-04 全面刷新）

上700pxが写真、その下がクリームの帯。文字はすべてクリームの上に置く。
写真の明るさに関係なく文字が読める、という一点でこの形を選んだ。

廃止したもの: 深緑の角丸枠／白いカード／吹き出し／円形に切り抜いた顔／
スペック4行目の「うまれた年」。囲みが三重になっていて賞状に見えていた。

まろ・レオの投稿（手作業）も同じ数値で作る。寸法と色は
`docs/mitakecats_card_template.png` を参照。

## 将来の拡張（今はやらない）

- 写真をGoogle Driveのフォルダから読む（今はリポジトリに置く運用）。
